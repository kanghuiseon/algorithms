#include <stdio.h>
#include <stdlib.h>

typedef struct Node{
  int data;
  struct Node *next;
}node;

node *head;

void insert(int N){
  node *t;
  int i;
  t = (node*)malloc(sizeof(node));
  t->data = 1;
  he
  for(i=2; i<=N; i++){
    t->next = (node*)malloc(sizeof(node));

  }

}
