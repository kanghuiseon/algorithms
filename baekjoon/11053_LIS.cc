#include <iostream>
#include <stdio.h>
using namespace std;

int main(){
  int num;
  int nums[1004];
  int check[1004];
  cin >> num;
  for(int i=0; i<num; i++){
    cin >> nums[i];
  }
  for(int j=0; j<num; j++){
    for(int k=0; k<num; k++){
      if(nums[j] < nums[k]){
        
      }
    }
  }
  return 0;
}
